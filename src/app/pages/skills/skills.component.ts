import { Component } from '@angular/core';

@Component({
  selector: 'app-skills-page',
  templateUrl: './skills.component.html',
})
export class AppSkillsPageComponent {
  code: string = `console.log(skills page)`;
}
